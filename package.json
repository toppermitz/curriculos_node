{
  "name": "curriculos_node",
  "version": "2.0.0",
  "description": "Webservice de currículos da Server",
  "main": "server.js",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "dev": "NODE_ENV=development node --watch --env-file-if-exists=.env server.js",
    "test": "node --test"
  },
  "author": "Anderson Dapper Rocha",
  "license": "ISC",
  "dependencies": {
    "express": "^5.2.1",
    "express-rate-limit": "^8.5.1",
    "mongodb": "^7.2.0",
    "pino": "^10.3.1",
    "pino-http": "^11.0.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "pino-pretty": "^13.1.3"
  }
}
